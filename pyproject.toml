[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "whisnake-circuitpython"
version = "0.1.0a1"
description = "CircuitPython stdlib flavor for Whisnake"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/whisnake_circuitpython"]
